import {
    
} from '../actions/constants';

const initialState = {
   
};

// function rootReducer(state = initialState, action) {
//     switch(action.type) {
//         case 
//         default:
//             return state;
//     }
// }

export default rootReducer;
